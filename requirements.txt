# Legacy compatibility requirements for running both components in one environment.
# Preferred setup now: install each component independently via
#   uv sync --project FrontendService
#   uv sync --project markedataservice

customtkinter>=5.2,<6
flask>=2.3,<4
matplotlib>=3.8,<4
numpy>=1.26,<3
pandas>=1.5,<3
python-dotenv>=1.0,<2
requests>=2.31,<3
tkcalendar>=1.6,<2
yfinance>=0.2.18,<1
