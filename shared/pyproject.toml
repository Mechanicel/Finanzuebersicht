[project]
name = "finanzuebersicht-shared"
version = "0.1.0"
description = "Shared models and utilities for finanzuebersicht services"
requires-python = ">=3.12"
dependencies = ["pydantic>=2.7,<3"]

[build-system]
requires = ["hatchling>=1.24"]
build-backend = "hatchling.build"
