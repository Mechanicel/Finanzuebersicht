# Compatibility requirements for markedataservice only.
# Source of truth: /pyproject.toml (dependency group: markedataservice)
# Preferred install from repo root:
#   uv sync --group markedataservice

flask>=2.3,<4
yfinance>=0.2.18,<1
pandas>=1.5,<3
